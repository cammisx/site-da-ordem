# Cleanup Notes (auto)
- Added src/data/atributos.json with agreed IDs (FOR/DES/INT/PRE/VIG).
- Added src/lib/pericias.js helpers for attribute lookup, DT formatting and "clique aqui" link text normalization.
- Added src/components/panels/PericiaPanel.jsx and wired it into the main painel when category is "pericias".
- Added minimal CSS for badges/test list to src/styles/pages/login.css.

No files were deleted automatically to avoid breaking references; if you want, we can remove unused public/react svg assets next.
